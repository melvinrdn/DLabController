Configuration
=============

Configuration system
--------------------
Application configuration is loaded via ``dlab.boot.get_config()``.

Configuration files are typically written in YAML format and may include:
- hardware connection parameters
- scan settings
- UI-related defaults

